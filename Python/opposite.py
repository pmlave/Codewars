# Simple function returns the opposite of the number passed in
def opposite(number):
  return number * -1
  
# SAMPLE TESTS
assert opposite(1) == -1
assert opposite(30) == -30
assert opposite(-10) == 10
assert opposite(-45) == 45